
{{ form_start(form) }}
    <div>{{ form_errors(form) }}</div>
    <div>
        {{ form_label(form.name, 'Digite o nome', {'label_attr': {'class': 'fw-bold'}}) }}
    </div>
    <div>
        {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div>
        <button class="btn btn-primary" type="submit">Save</button>
    </div>
{{ form_end(form) }}