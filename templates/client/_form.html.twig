{{ form_start(form) }}
    <div>{{ form_errors(form) }}</div>
    <div>
        {{ form_label(form.name, 'Informe um nome', {'label_attr': {'class': 'fw-bold py-3'}}) }}
    </div>
    <div>
        {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div>
        {{ form_label(form.address, 'Informe um Endereço', {'label_attr': {'class': 'fw-bold py-3'}}) }}
    </div>
    <div>
        {{ form_widget(form.address, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div>
        {{ form_label(form.phone, 'Informe um Telefone', {'label_attr': {'class': 'fw-bold py-3'}}) }}
    </div>
    <div>
        {{ form_widget(form.phone, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div>
        {{ form_label(form.accounts, 'Selecione uma conta', {'label_attr': {'class': 'fw-bold py-3'}}) }}
    </div>
    <div>
        {{ form_widget(form.accounts, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="d-flex align-items-center justify-content-between py-5">
        <a class="btn btn-outline-dark" href="{{ path('app_client_index') }}">Voltar para a Lista</a>
        <button class="btn btn-dark" type="submit">{{ button_label|default('Salvar') }}</button>
    </div>
{{ form_end(form) }}